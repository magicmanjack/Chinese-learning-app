TODO:
    new plan
    How about ec2 instance for database and 5$ instance for webserver front end.
    Then can connect with private network.
    use one $7 lightsail instance to serve web and API
    use one $5 instance to serve db
    NOT GONNA WORK - unless manually set up private IP connections. But then anyone else could potentially
    query the database. Look into cheap db options. Or db authentication? Or one single instance.
